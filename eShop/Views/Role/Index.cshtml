@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<br />
<br />
<div class="section-padding">
    <h2>Availlable Roles</h2>
    <section class="col-sm-6">
        <table class="table-bordered">
            @Html.ActionLink("Create a new Role", "Create", "Role", new { @class = "btn btn-primary btn-sm btn-block" });
        </table>
    </section>

  @*<section class="col-sm-6">
            <table class="table-bordered">
                @Html.ActionLink("Assign Role To User", "RegisterRole", "Account", new { @class = "btn btn-primary btn-sm btn-block" });
            </table>
        </section>*@
    
    <br />
    <br />
    <table class="table table-bordered table-hover">
        <tr>
            <th>Role Name</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <th>@item.Name</th>
            </tr>
        }
    </table>

</div>

